'use babel';

import LanguageTenableauditsView from '../lib/language-tenableaudits-view';

describe('LanguageTenableauditsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
